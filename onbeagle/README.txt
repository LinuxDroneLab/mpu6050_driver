inserire il tutto in /root/pid
copiare in questa cartella anche le componenti firmware: pru-rpmsg-bridge.out e pru-components-bridge.out
eseguire il file 'go.sh' che:
1) compila il driver linux
2) installa il firmware
3) esegue il tail di /var/log/syslog

